# Chrome DevTools MCP Server

浏览器自动化工具，支持截图、点击、表单填写、页面内容提取等。

## 前置条件

需要安装 playwright：
```bash
pip install playwright
playwright install chromium
```

## 可用工具

- `screenshot`: 页面截图
- `click`: 点击元素
- `get_content`: 获取页面文本
